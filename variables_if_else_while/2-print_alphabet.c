#include <stdio.h>

/**
 * main - Entry point
 * Description: Prints alphabet in lowercase
 * Return: always 0 succes
*/
int main(void)

{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	putchar (i);
	putchar ('\n');
	return (0);
}
