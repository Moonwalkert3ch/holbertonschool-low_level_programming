#include <stdio.h>

/**
 * main:  Prints alphabet in lowercase
 * RETURN: 0 always succes
 *
*/

int main(void)

{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	putchar (i);
	putchar ('\n');
	return (0);
}
