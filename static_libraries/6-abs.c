#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @a: number desired.
 * Return: always 0.
*/
int _abs(int a)
{
	if (a > 0)
	return (a);
	else if (a < 0)
	return (-a);
	else
	return (0);
}
